export interface Booking {
  id: number;
  username: string;
  trainName: string;
  departureTime: string;
  seatNumber: number;
}

export interface PersonalBooking {
  id: number;
  trainName: string;
  departureTime: string;
  seatNumber: number;
}